[pytest]
markers =
    layer1: Hardware and host OS
    layer2: Container health
    layer3: GStreamer pipeline
    layer4: API contract
    layer5: Behavioral / visual
testpaths = tests
addopts = -v --tb=short
